# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch('.serverlock')
  watch(%r{assets/.+})
  watch(%r{templates/.+})
  watch(%r{config/locales/.+\.yml})
end
